\documentclass[12pt]{article}
\usepackage{russ}
\usepackage{amsmath}
\begin{document}

Писать $\tilde i$ некрасиво; лучше писать так: $\tilde\imath$.

Правильно $\Hat{\Hat Z}$, а не $\hat{\hat Z}$.

По формуле $\mathrm{tg} x$ буква $x$ слишком близка к знаку тангенса. А вот в формуле $\sin x$ пробелы правильные.

Множество особенностей многообразия $X$ обозначается $X_{\mathrm{sing}}$.

Раньше вместо $\Gamma^k_{ij}$ писали $\left\{ij\atop k\right\}$.


$$\textbf{Задание: набрать текст и формулы}$$
$$\textbf{Скобки переменного размера}$$

$$M(f) = \left.\left( \int\limits_a^b f(x)dx \right) \right/ (b - a)$$

$$\int\limits_a^b \frac{1}{2}(1 + x)^{-3/2}dx = \left. - \frac{1}{\sqrt{1 + x}}\right|_a^b$$

\textbf{Обратите внимание, что следующие формулы отличаются}

$$||x + 1| - |x - 1||$$

$$\bigl||x + 1| - |x - 1|\bigl|$$

\textbf{Обратите внимание, что следующие формулы отличаются}

$$\left( \sum\limits^{n}_{k=1}x^{k}\right)^{2}   ( \sum\limits^{n}_{k=1}x^{k})^{2}$$

Множество ${x | x \ni x}$ существовать не может. В этом состоит парадокс Рассела.

$$\boxed{\int\int_{R^{2}} e^{-(x^{2}+y^{2})} dx dy = \pi}$$

$$\overline{a_{n}a_{n-1}...a_{1}a_{0}} = 10^{n}a_{n} + ... + a_{0}.$$

$\widehat{f * g} = \hat{f} \cdot \hat{g}$. \hskip 50pt Рассмотрим вектор $\overrightarrow{AB}$.

$$
\centerline{\textbf{Одно над другим(простейшие случаи)}}
$$

$$\frac{2}{3}\ \ и \ \ \frac{2}{3}$$

$$2^{\frac{3}{5}} \ \ и \ \ 2^{\frac{3}{5}}$$

$$(^{12}_{7}) = 792$$

$$\left(^{n}_{k}\right) = \frac{n!}{k!(n - k)!}$$

$$\frac{\sum\limits^{n}_{i=1}|x_{i}||y_{i}|}{\left(\sum\limits^{n}_{j=0} |x_{j}|^{\frac{1}{1-\sigma}}\right)^{1 - \sigma} \left(\sum\limits^{n}_{j=1} |y_{i}|^{\frac{1}{\sigma}}\right)^{\sigma}} \leq 1 $$


$$\mbox{sp} \hskip 2pt A \subseteq \bigcup\limits^{n}_{i = 1} S(a_{ii},r_{i}),\;\;\; r_{i} = \min\{p_{i},q_{i}\}$$.

$$||x||_{\frac{1}{2}} = \left( \sum\limits^{n}_{i = 1} |x_{i}|^{2}\right)^{\frac{1}{2}} $$.

$$e^{tA} = I + \sum\limits^{\infty}_{k=1}\frac{t^{k}A^{k}}{k!}$$,

$$\lim\limits_{0<t\rightarrow 0} \frac{\ln||e^{tA}||}{t} = \lim\limits_{0<t\rightarrow 0} \frac{\ln||I + tA||}{t} \ \ , \ \ ||A|| = \max\limits_{x\neq0} \frac{||Ax||}{||x||}$$.


\end{document} 